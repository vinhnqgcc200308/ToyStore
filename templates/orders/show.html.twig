{% extends 'base.html.twig' %}

{% block title %}Orders{% endblock %}

{% block body %}
    <h1>Orders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>OrderDate</th>
                <td>{{ order.OrderDate ? order.OrderDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>OrderDelivery</th>
                <td>{{ order.OrderDelivery ? order.OrderDelivery|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>OrderStatus</th>
                <td>{{ order.OrderStatus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_orders_index') }}">back to list</a>

    <a href="{{ path('app_orders_edit', {'id': order.id}) }}">edit</a>

    {{ include('orders/_delete_form.html.twig') }}
{% endblock %}
